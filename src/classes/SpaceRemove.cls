public with sharing class SpaceRemove {
	public static void spaceRemove(String inputString){
		String[] myList = inputString;
		
		for(String  s : myList){
			/*if(s == ' '){
				s = myList
			}*/
			system.debug(s);
		}
		//system.debug(myList);
	}
    
}