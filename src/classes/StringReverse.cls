public with sharing class StringReverse {
	public static void StringReverse(String inputString){
        //String rev = inputString.reverse();
		system.debug('Reverse of Input String is'+inputString.reverse());
		//return(reverse(inputString));
	}
    
}